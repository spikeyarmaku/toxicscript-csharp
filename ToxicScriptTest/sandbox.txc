; (lambda x (* x x) 1)

; (nth 0 (map (+ 1) (cons 1 ())))

(nth 2 (map (lambda x (* x x)) (list (1 2 3 4 5))))

; (let fact_base
;     (lambda rec
;         (lambda n
;             ((= n 1)
;                 n
;                 (* n (rec rec (- n 1))))))
;     (let fact
;         (lambda n
;             (fact_base fact_base n))
;         (fact 5)))

; (letrec fact
;     (lambda n
;         ((= n 1)
;             1
;             (* n (fact (- n 1)))))
;     (fact 5))